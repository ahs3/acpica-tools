#!/bin/sh
set -e

BINDIR=/usr/bin
case $1 in
remove|disappear)
	if [ ! -e $BINDIR/acpixtract-acpica ]
	then
    	    update-alternatives --remove acpixtract $BINDIR/acpixtract-acpica
	fi
	;;
*)	;;
esac

#DEBHELPER#

exit 0
